<?xml version="1.0"?>
<launch>

  <arg name="paused" default="false"/>
  <arg name="gui" default="true"/>

  <!-- Start Sim -->
  <include file="$(find gazebo_ros)/launch/empty_world.launch">
    <arg name="world_name" value="worlds/empty.world"/>
    <arg name="paused" value="$(arg paused)"/>
    <arg name="gui" value="$(arg gui)"/>
  </include>

  <!-- Spawn Table -->
  <!--<param name="table_description" command="$(find xacro)/xacro.py $(find push_vs_grasp)/models/urdf/table_colour.gazebo.xacro"/>-->
  <!--<node name="mybot_spawn" pkg="gazebo_ros" type="spawn_model" output="screen" args="-urdf -param table_description -model table" />-->
  
   
</launch>
